export function createImageCarousel(events){
    const arrayImage = document.createDocumentFragment
    if(events.length > 4){
        for(let i = 0; i > 6; i++){
            
        }
    }else{
    }
}